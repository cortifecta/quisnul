args = 'last_submission_date'

if args == 'last_submission_date':
    # This block will execute if args is equal to 'last_submission_date'
    print("Processing last submission date...")
else:
    # This block will execute if args is not equal to 'last_submission_date'
    print("Args is not 'last_submission_date'")
